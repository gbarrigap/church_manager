<nav class="navbar navbar-expand-lg navbar-light bg-light mb-3">
  <span class="navbar-brand">Church Manager</span>
  <ul class="navbar-nav mr-auto">
    <li class="nav-item"><%= link_to t('general.home'), root_path, { class: 'nav-link' } %></li>
    <li class="nav-item dropdown">
      <a class="nav-link dropdown-toggle" href="#" id="navbar-dropdown-baptisms" role="button" data-toggle="dropdown" aria-haspopup="true" aria-expanded="false"><%= t('general.baptisms') %></a>
      <div class="dropdown-menu" aria-labelledby="navbar-dropdown-baptisms">
        <%= link_to t('general.new_baptism'), new_baptism_path, { class: 'dropdown-item' } %>
        <%= link_to t('general.find_baptism'), baptisms_path, { class: 'dropdown-item' } %>
      </div>
    </li>
  </ul>
  <ul class="navbar-nav ml-auto">
    <li class="nav-item dropdown">
      <a class="nav-link dropdown-toggle" href="#" id="navbar-dropdown-account" role="button" data-toggle="dropdown" aria-haspopup="true" aria-expanded="false"><%= t('general.account') %></a>
      <div class="dropdown-menu dropdown-menu-right" aria-labelledby="navbar-dropdown-account">
        <%= link_to t('general.profile'), church_path, { class: 'dropdown-item' } %>
        <%= link_to t('general.settings'), church_path, { class: 'dropdown-item' } %>
        <div class="dropdown-divider"></div>
        <%= link_to t('general.log_out'), logout_path, { class: 'dropdown-item' } %>
      </div>
    </li>
  </ul>
</nav>
